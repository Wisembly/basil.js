(function () {
	describe('Mathieu should write some tests', function () {
		it('should have no excuses not to write them now', function () {
			expect(window.Basil).to.be.a('function');
		});
	});
}());
